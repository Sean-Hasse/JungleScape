This file is for planned changes, anyone can contribute

- Add Boss to Enemy, or as another class (unlikely).
- Fix enemy ledge detection.
- Make the Player.Move() y-speed = 0 if they hit their head.
- Fix artificial gravity in Player.Move().
- Implement FireArrow(Texture 2D, List<GameObject> objects) into Game1.
- Fix up code preventing FireArrow from being spammed.
- Make the spider hurt the player.
- Draw different Player images depending on which enum state Player.Aim() set it to.
- Required drawings for images: the Player aiming forward, diagonal and up; Arrows.